name: Problem 001 Workflow
on:
  push:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        run: actions/checkout@v3
      - name: Say Hello
        run: echo "Hello world"
